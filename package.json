{
  "name": "postgres-setup",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup-db": "node scripts/setup.js",
    "start-db": "docker-compose up -d",
    "stop-db": "docker-compose down",
    "init-db": "docker-compose down && docker-compose up -d",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
